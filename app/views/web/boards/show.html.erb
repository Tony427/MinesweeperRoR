<div class="container">
  <h2><%= @board.name %></h2>
  <p class="text-muted">Created by: <%= @board.email %> on <%= @board.created_at.to_formatted_s(:long) %></p>

  <div class="mt-4">
    <h4>Board State:</h4>
    <div class="board-grid" style="display: grid; grid-template-columns: repeat(<%= @board.width %>, 2rem); gap: 2px; margin-top: 1rem;">
      <% JSON.parse(@board.board_data).each do |row| %>
        <% row.each do |cell| %>
          <div class="cell" style="width: 2rem; height: 2rem; border: 1px solid #ccc; display: flex; justify-content: center; align-items: center; background-color: #f0f0f0;">
            <%= cell == 'X' ? '💣' : '□' %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>

  <div class="mt-4">
    <%= link_to 'Back to Home', root_path, class: 'btn btn-secondary' %>
  </div>
</div>
