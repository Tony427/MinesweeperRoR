# Ruby on Rails Environment Variables Template
# Copy this file to .env and fill in the actual values

# Rails Configuration
# Generate with: bundle exec rails secret
SECRET_KEY_BASE=your_secret_key_base_here

# Database Configuration (Production/Staging)
# DATABASE_URL will be set automatically by Heroku
# DATABASE_URL=postgres://username:password@hostname:port/database_name

# Redis Configuration (if using)
# REDIS_URL will be set automatically by Heroku
# REDIS_URL=redis://localhost:6379/0

# Application Settings
RAILS_ENV=development
RAILS_LOG_LEVEL=info

# Heroku Configuration (for deployment)
# RAILS_MASTER_KEY will be set from config/master.key content
# RAILS_SERVE_STATIC_FILES=true
# RAILS_LOG_TO_STDOUT=enabled

# Third-party Service Keys (examples)
# Replace with your actual service credentials
# SMTP_USERNAME=your_smtp_username
# SMTP_PASSWORD=your_smtp_password
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# Security Configuration
# ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Development/Testing Configuration
# TEST_DATABASE_URL=sqlite3:db/test.sqlite3

# Additional Notes:
# 1. Never commit .env files with actual values to version control
# 2. Use Rails encrypted credentials for production secrets
# 3. Set environment variables in your hosting platform (Heroku, etc.)
# 4. Keep this .env.example file updated as a reference