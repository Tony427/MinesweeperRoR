# Ruby on Rails Environment Variables Template
# Copy this file to .env and fill in the actual values for local development

# =============================================================================
# Rails Core Configuration
# =============================================================================

# Rails Environment (development, staging, production)
RAILS_ENV=development

# Secret Key Base - Generate with: bundle exec rails secret
# In production/staging, this is managed via Rails encrypted credentials
SECRET_KEY_BASE=your_secret_key_base_here

# Rails Master Key (for encrypted credentials)
# In production/staging, this is set automatically from config/master.key
# RAILS_MASTER_KEY=your_master_key_here

# Rails Logging
RAILS_LOG_LEVEL=info
# RAILS_LOG_TO_STDOUT=enabled  # Uncomment for Heroku deployment

# =============================================================================
# Database Configuration
# =============================================================================

# Local Development (SQLite)
# Default: db/development.sqlite3
# DATABASE_URL=sqlite3:db/development.sqlite3

# Staging/Production (PostgreSQL)
# For Heroku, DATABASE_URL is set automatically
# DATABASE_URL=postgres://username:password@hostname:port/database_name

# Test Database
# TEST_DATABASE_URL=sqlite3:db/test.sqlite3

# =============================================================================
# Server Configuration (Cross-platform)
# =============================================================================

# Server Host and Port (used by development scripts)
# HOST=0.0.0.0          # Default host for development server
# PORT=3001             # Default port for local development (Docker uses 3000)

# Static Files (for production deployment)
# RAILS_SERVE_STATIC_FILES=true

# =============================================================================
# External Services Configuration
# =============================================================================

# Redis (if using for caching, sessions, or background jobs)
# REDIS_URL=redis://localhost:6379/0

# Email Configuration (SMTP)
# SMTP_USERNAME=your_smtp_username
# SMTP_PASSWORD=your_smtp_password
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587

# Cloud Storage (AWS S3 example)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-s3-bucket-name

# =============================================================================
# Security Configuration
# =============================================================================

# Allowed Hosts (for production)
# ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Force SSL (for production)
# FORCE_SSL=true

# =============================================================================
# Development/Testing Environment Variables
# =============================================================================

# Ruby Version Consistency
# RBENV_VERSION=3.4.5  # Ensure consistent Ruby version across environments

# Bundle Configuration
# BUNDLE_PATH=vendor/bundle
# BUNDLE_WITHOUT=production

# Docker Development
# DOCKER_BUILDKIT=1     # Enable Docker BuildKit for faster builds

# =============================================================================
# Platform-specific Notes
# =============================================================================

# Windows Development:
# - Use PowerShell or Command Prompt
# - Scripts: scripts\start.bat, scripts\local.bat, scripts\docker.bat
# - Ruby installed via RubyInstaller with MSYS2 toolchain

# Unix-like Development (Linux/macOS):
# - Use Bash shell
# - Scripts: ./scripts/start.sh, ./scripts/local.sh, ./scripts/docker.sh
# - Ruby installed via rbenv, rvm, or system package manager

# =============================================================================
# Deployment Environment Variables (Heroku)
# =============================================================================

# These are set automatically by Heroku or manually via Heroku CLI:
# - DATABASE_URL (PostgreSQL add-on)
# - REDIS_URL (Redis add-on, if used)
# - RAILS_MASTER_KEY (set from config/master.key)
# - RAILS_SERVE_STATIC_FILES=true
# - RAILS_LOG_TO_STDOUT=enabled

# =============================================================================
# Important Security Notes
# =============================================================================

# 1. NEVER commit .env files with actual values to version control
# 2. Use Rails encrypted credentials (rails credentials:edit) for production secrets
# 3. Set environment variables in your hosting platform (Heroku, etc.)
# 4. Keep this .env.example file updated as a reference for developers
# 5. Use strong, unique values for all secret keys and passwords
# 6. Rotate secrets regularly in production environments